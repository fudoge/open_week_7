# Merge

## Merging timelines
```bash
git merge <baguette의 커밋해시>
git merge <coffee의 커맷해시>
```

## Contradictions
```bash
git merge pancakes
git merge muesli

# 충돌 해결 후..
git add .
git commit -m "Resolved Confliction"
```
